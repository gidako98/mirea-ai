# Configuration

`config.json` contains reproducible project settings: paths, train/test split, random seed, service port and model hyperparameters.

`configs/.env.example` lists optional environment variables for local service runs. Real `.env` files are intentionally ignored and must not be committed.
